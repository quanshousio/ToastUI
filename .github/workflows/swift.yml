name: Swift

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v2
    - name: Xcode build
      run: |
        xcodebuild -version
        xcodebuild clean build -sdk iphoneos -scheme ToastUI
